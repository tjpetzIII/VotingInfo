# Implementation Plan: VOT-27

Add a reusable invite component/page using `buildShareUrl`, Web Share API detection, and clipboard fallback. Keep all copy in English and Spanish catalogs and avoid collecting or exposing context without an explicit checkbox choice.
