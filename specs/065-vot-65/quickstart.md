# Quickstart

Run `cd backend && cargo fmt --check && cargo test && cargo clippy -- -D warnings`, then `cd frontend && yarn lint && npx tsc --noEmit && yarn test && yarn build`. Tests must use fixed instants and mocked upstreams.
