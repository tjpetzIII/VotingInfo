# Research

The current `ElectionDate` stores only label, category, date, and server-local `days_remaining`; scrapers also strip deadline prose to dates. `StateImportantDate` has only election year, allowing unrelated same-year records. Official USAGov and Pennsylvania guidance distinguish postmarked/received actions and explicit local cutoffs.
