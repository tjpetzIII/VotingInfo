# Validation

Run `CARGO_TARGET_DIR=/Users/tpetz/Code/AI/votingApp/backend/target cargo check --locked`, `cargo test --locked`, `cargo clippy --locked -- -D warnings`, and `cargo fmt --check` from `backend/`.
