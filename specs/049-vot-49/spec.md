# VOT-49 Spec: TypeScript 7 evaluation

Evaluate the TypeScript 7 tsgo toolchain against Next 16 and the repository's strict configuration. Adopt only after compatibility is demonstrated; a documented spike is acceptable because this is a major compiler rewrite.
