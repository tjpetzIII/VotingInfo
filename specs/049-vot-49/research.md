# Research

The integrated frontend declares TypeScript `^6` and resolves 6.0.3. TypeScript 7's native compiler rewrite needs a dedicated compatibility spike; no safe migration evidence is available in this environment.
