# Implementation Plan: VOT-26

Create a client-only `/voted` page with a canvas badge preview and PNG download. Use react-intl for all app copy, constrain user text to the badge and share query, and build the X URL with encoded election text and a static app URL. No address or API calls are involved.
