# Data model: VOT-33

No application entities or state transitions change. The dependency manifest declares the accepted version floor; the Yarn lock records the exact version, download URL, integrity hash, and transitive dependencies. Every PostCSS selector must lead to one patched installed version.
