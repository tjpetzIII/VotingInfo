# Contracts

This feature exposes no runtime API, CLI, or data contract. The documentation note is the maintainer-facing evidence contract: commands, target/feature scope, observed graph output, conclusion, and invalidating triggers must remain together.
