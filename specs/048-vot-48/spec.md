# VOT-48 Spec: Resolver major baseline

Verify `@hookform/resolvers` is on a current 5.x release compatible with the app's React Hook Form 7 and Zod 4 dependencies, preserving login validation behavior.
