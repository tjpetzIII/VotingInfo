# Data model: VOT-34

No application entities or persisted state change. The auditable dependency state consists of the manifest requirement, Yarn resolution policy, lockfile package records, and installed package records.
