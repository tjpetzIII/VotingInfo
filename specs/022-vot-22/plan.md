# Implementation Plan: VOT-22

Add a typed frontend `subscribeReminder` boundary and reusable `ReminderSignup` component. Submit only after explicit consent; use current address context when available, preserve state, and show localized actionable errors. Backend provider remains the VOT-21 no-send boundary.
