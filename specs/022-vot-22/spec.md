# VOT-22 Reminder signup

## User Story 1 (P1)
As a voter, I can explicitly opt in to election reminders with a validated email and my existing address context, and unsubscribe with an opaque token.

Requirements: bilingual accessible form, idempotent signup, no credentials, typed API boundary, and no live outbound email in tests.
