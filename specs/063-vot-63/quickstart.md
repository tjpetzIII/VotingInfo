# Validation

Run `cargo fmt --check`, `cargo test`, and `cargo clippy -- -D warnings` in `backend/`; run `yarn lint`, `yarn tsc --noEmit`, `yarn test`, and `yarn build` in `frontend/`. Use mocked Civic fixtures for category, mail-only, and missing-field cases.
