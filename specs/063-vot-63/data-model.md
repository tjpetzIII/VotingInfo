# Data model

`PollingLocation` remains the compatible base contract and gains additive category metadata. `VoterInfoResponse` adds `early_vote_sites`, `drop_off_locations`, `mail_only`, and `voting_location_finder_url`. All fields preserve nullability from Civic and coordinates remain optional.
