# Implementation Plan: VOT-68

Add a typed static resource registry for states and DC, a bilingual accessible guide page, and contextual links from voter-info and polling. Clearly label fallback/unknown review metadata and avoid eligibility conclusions. Add deterministic registry coverage tests.

## Constitution Check

Pass: no sensitive data is collected, official data is mapped into project types, and the shared shell and translation conventions are used.
